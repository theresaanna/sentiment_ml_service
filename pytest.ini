[pytest]
testpaths = tests .
addopts = -vv -rs
markers =
    integration: marks tests that hit external services or require secrets (deselect with '-m "not integration"')
    external: marks tests that call external APIs or Modal deployment endpoints
filterwarnings =
    ignore::PendingDeprecationWarning:starlette.formparsers
